package ir.ac.tick.soft.demo.mock;

/**
 * The EmailSender class is also an external system from a third-party mass email provider.
 */
public class EmailSender {

    // Uses an SMTP server to send email to specified customer
    public void sendEmail(Customer customer) {
    }
}
